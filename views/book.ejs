<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta http-equiv="content-type" content="text/html"; charset="utf-8">
        <link type="text/css" href="./style/image.css" rel="stylesheet">
        <title>node.js</title>
    </head>
    
    <body>
        <head>
            <h1><%=title %></h1>
        </head>

        <% images.forEach(image => { %>
            <img src=<%= image %> class="card-image-top" alt="">
        <% }) %>
    </body>
</html>